// Kept in sync with the CSS custom properties in index.css. Used where a
// literal color value is needed (e.g. inside Leaflet divIcon HTML strings)
// rather than a CSS class.
export const ACCENT_COLOR = '#2f7a4d';
export const DARK_COLOR = '#26261f';
